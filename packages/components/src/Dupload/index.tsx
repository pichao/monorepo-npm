import { Button } from 'antd';

export default () => {
    return (
        <div className="yyds-p-upload">
            这是上传组件
            <Button type={'primary'}>上传 </Button>
        </div>
    );
};
